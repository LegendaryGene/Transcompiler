#include <bits/stdc++.h>
using namespace std;

class Bit_operators{
    public:
        void Not(string &, int &, int &);

        void And(string &, int &, int &);

        void Or(string &, int &, int &);

        void Nand(string &, int &, int &);

        void Xor(string &, int &, int &);
};